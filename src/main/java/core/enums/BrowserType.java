package core.enums;

public enum BrowserType {
	
	//“We are only allowing these three browsers. No typo-prone strings allowed!”
	//Using enums ensures that only valid, predefined values are used. The compiler will catch mistakes early.
	//clean code because it is switch case friendly.
	
	CHROME,
	FIREFOX,
	EDGE

}
